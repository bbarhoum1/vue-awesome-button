import HelloWorld from "./components/HelloWorld.vue";

// Define the type for HelloWorld component
export interface HelloWorldProps {
  msg: string;
}

// Export the component as a named export
export { HelloWorld };
